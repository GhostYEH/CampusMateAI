# 此目录存放 CNN 表情识别模型及配置,由 cnn-training 分支提供:
#   - expression_model.tflite   (TFLite/LiteRT 模型)
#   - labels.json               (七类表情标签顺序)
#   - preprocess.json           (预处理参数: 输入尺寸/通道/归一化/缩放模式)
#   - model_card.json           (可选: SHA-256 / 输入输出张量说明 / 训练元数据)
#
# 当前 agent/cnn-flutter-runtime 分支仅提供运行时:
#   - 模型缺失时 Release 显示明确错误(不静默回退 Mock)
#   - Debug 模式下可通过 USE_MOCK_EXPRESSION=true 开关启用带标识的 Mock
#
# 模型就绪后放入此目录,运行时自动加载。
