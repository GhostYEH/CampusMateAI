# 03 · 待办：剩余工作与切片计划

按优先级排列。每完成一个切片就提交一次并做真实浏览器验收（见 `05-工程规范.md`）。

**2026-09-20 目标校正**：用户明确要求模板前端及后端能力完整迁入“我的课程”。
P1～P5 是已有适配层的第一批补齐工作，不是完整移植的全部范围。
已有运行时缺口必须列为待补依赖，不能仅因当前未实现就永久排除。
保留 CampusMate 的单一登录、课程权限、资料事实源与 FastAPI 网关；模板的页面、
交互和后台业务逐项对齐，框架、身份和持久化适配处记录差异。

---

## 优先级 0 · “我的课程”首页等价迁移（P0-A，必须先做）

### 实测结论（2026-09-20）

不能把“进入课堂 / 最近内容 / 更多学习工具 / 我的课程列表”四个 CampusMate 业务卡片
当成模板首页的等价实现。模板根页的首屏是一个全屏、低干扰的生成工作台：浅灰蓝背景、
右上角轻量设置条、居中的 OpenMAIC 字标与标语、约 800px 宽的单一输入卡片，以及下方
“最近课堂”的分隔条和课堂卡片库。现有页面虽然借用了部分颜色和圆角，却改变了信息层级、
DOM 结构和主路径；它不符合“照着模板移植”的目标。

该结论来自两个独立浏览器上下文的同分辨率实测（参考项目根页与 CampusMate `/courses`），
不是静态截图猜测。后续每次视觉变更都必须复跑这项对照，不可以只验收工作台内部。

### P0-A 的产品与后端契约

1. `/courses` 在 OpenMAIC 壳内渲染模板根页的首屏结构：背景装饰、字标/标语、问候和
   Agent 条、`textarea`、工具条、进入课堂按钮、最近课堂分隔条和课堂卡片库。不要保留
   “进入课堂”说明卡、更多学习工具 Tab、工作台 CRUD 表单和 31 门课长列表来占据首屏。
   课程切换保留为一个可访问的紧凑控件，且不覆盖模板输入区。
2. 输入必须产生真实课程绑定的课堂：提交时由 FastAPI 根据当前用户和选中课程，原子地
   解析/创建该课程的首页工作台，再调用受管服务生成 stage；浏览器只调用 FastAPI。
   请求保留 `Idempotency-Key`，重复提交只能恢复同一工作台/任务，不能生成两个首页工作台。
   成功或异步任务完成后直达该工作台的课堂学习态；503、权限错误和任务失败留在首页给出
   可重试的局部错误，不能丢失输入内容。
3. 模板工具条的每个可见控件都要有真实行为和后端能力门：当前已接通的资料、Provider
   状态、模式/角色可复用现有接口；尚未接通的附件、联网搜索、语音输入等不得伪装成可用。
   视觉上要保留模板的布局槽位，功能状态必须明确且可访问。
4. 最近课堂来自现有 course-bound recent API，卡片进入对应的 CampusMate 工作台/课堂；
   当前用户的普通课程库移入不抢占首屏的折叠/切换入口，仍使用既有课程事实源。

### P0-A 的实现边界与验收

- 前端预计涉及 `ParityPages.jsx`、`components/openmaic/OpenMAICHome.jsx`（可拆成首页专用
  组件）、`features/openmaic/**`、`data/api.js`、`styles.css` 与现有 OpenMAIC 首页测试。
  不引入全局 Tailwind preflight，不复制参考项目二进制 logo；字标由 CSS/现有图标实现。
- 后端预计涉及 `openmaic_generation.py`、`fusion_client.py`、受管服务 generation/workspace
  路由及三层测试。复用现有 workspace、job、provider 和 stage 生成实现，不能让浏览器
  直连 `:4010`，也不能以整份 DSL 写回代替命令/任务链路。
- 自动化要新增 P0 首页契约测试和 FastAPI/服务端的“首页提交幂等、课程归属、失败恢复”测试。
- 真实浏览器必须同时启动参考项目与 CampusMate，分别建立独立 context，在 1440/1600 桌面
  与 320px 下抓图、量出 hero/composer/最近课堂几何；记录页面级横向溢出、`pageerror`、
  `console error`、>=400 API 响应的精确数量。验收必须真的输入主题、提交、等待/恢复任务并
  到达课程课堂；截图只放临时目录，不提交。

完成 P0-A 后才能继续 P1-C。P1-A/P1-B 的编辑器工作不得回退，但它们不再是首页视觉对齐的
先决交付物。

---

## 优先级 1 · 工作台编辑器可视化（剩余最大的一块）

### 现状

`webreact/src/components/openmaic/StageEditorPanel.jsx` 已完成 P1-A：场景列表选择会在
`webreact/src/maic/edit/StageCanvasPreview.jsx` 中用真实 DSL 画布渲染当前幻灯片。
它仍保留场景 DSL JSON 的编辑入口；元素级的选中、拖拽与富文本编辑尚未完成。

参考项目对应的是 `components/edit/**`（66 文件 / 471 KB）与
`components/slide-renderer/Editor/**`（画布 + 拖拽/缩放/旋转手柄约 25 文件）。

### 移植层里**已经有的**编辑态资产（不要重复移植）

| 已有 | 位置 |
| --- | --- |
| 9 种只读元素渲染器 | `slide/components/element/*/Base*.jsx` |
| **编辑态变体（仅 3 种）** | `ChartElement/index.jsx`、`LineElement/index.jsx`、`TableElement/index.jsx` |
| 播放画布 | `slide/Editor/ScreenCanvas.jsx`、`RendererScreenCanvas.jsx`、`ScreenElement.jsx` |
| 特效层 | `slide/Editor/{Highlight,Spotlight,Laser}Overlay.jsx` |
| 画布背景 | `slide/Editor/Canvas/ViewportBackground.jsx` |
| 缩放包装 | `slide/Editor/ZoomWrapper.jsx` |
| 元素工具函数 | `slide/_compat/element.js`、`geometry.js`、`element-dom.js`、`image-clip.js`、`color.js` |

### 移植层里**还没有的**（要新建）

- `Editor/Canvas/index.tsx`（参考 434 行）——编辑画布本体
- `Editor/Canvas/Operate/**`——选中框与各类型操作手柄
- `Editor/Canvas/hooks/`——`useDragElement.ts`(404)、`useScaleElement.ts`(695)、
  `useRotateElement.ts`(135)、`useSelectElement.ts`(131)、`useMouseSelection.ts`(200)、
  `useDragLineElement.ts`(273)、`useMoveShapeKeypoint.ts`(139)、`useInsertFromCreateSelection.ts`(145)、
  `useDrop.ts`、`useCommonOperate.ts`、`useViewportSize.ts`
- `Editor/Canvas/AlignmentLine.tsx`、`GridLines.tsx`、`Ruler.tsx`、`MouseSelection.tsx`、`ShapeCreateCanvas.tsx`
- `Editor/EditableElement.tsx`、`Editor/Canvas/ElementCreateSelection.tsx`
- `components/element/ProsemirrorEditor.tsx`（553 行）与其余 6 种编辑态变体
- `lib/prosemirror/**`（16 文件）与 `lib/edit/**`（24 文件）

### 建议的切片顺序

每一片都**先读参考实现对应的文件，逐字对齐类名与 DOM 结构**，只做机械改写
（擦类型、换 import、去 `'use client'`、`next/*` 换掉、`t()` 换中文）。

#### 切片 A：编辑态渲染真实画布（只读）

- 目标：工作台编辑态里用移植的画布渲染当前场景的真实幻灯片，替掉现在的空白 16:9 盒。
- 参考：`components/slide-renderer/Editor/index.tsx`(18 行) →
  `Editor/Canvas/index.tsx`(434 行) 的**只读路径**；`components/edit/EditChromeRoot.tsx`(127 行)。
- 产物：`webreact/src/maic/edit/StageCanvasPreview.jsx` + `StageEditorPanel` 的当前场景选择与完整 stage 单次读取。
- 验收：编辑态截图里能看到真实幻灯片（`.base-element-text` 等节点存在），
  与课堂播放态渲染**同一份画布**。

#### 切片 B：元素选中与拖拽

- 参考：`Editor/Canvas/Operate/**`、`hooks/useSelectElement.ts`、`hooks/useDragElement.ts`。
- 先补齐服务端命令：当前 `STAGE_COMMANDS` 只有 stage/scene 级命令，不能把浏览器
  拖拽写回。新增 `slide.element.move`，字段为 `sceneId`、`elementId`、最终 `left`、`top`；
  它只能命中 `content.type === "slide"` 的既有元素，四个字段均严格校验，其他字段、
  actions 与场景顺序保持不变。它仍走现有 commands 路由、`prepareStage`、revision、
  `Idempotency-Key`、`If-Match` 与四层归属校验，不新开整份文档 PUT。
- 前端在 `editorModel` 提供同名命令构造与本地镜像；`StageCanvasPreview` 以现有
  `[data-maic-element-id]` 为命中点，点击选中，pointer drag 只在 pointerup 时把最终坐标
  加入命令缓冲。坐标按实际画布缩放换算；空白处取消选中。只支持带有限 `left/top` 的
  slide 元素，其他场景/元素保持只读并明确不显示操作框。
- 可视反馈只移植本切片需要的单选边框与拖动状态；多选、成组、对齐线、缩放、旋转、
  键盘微调、插入/删除全部留给 P1-C～E，不能借此写入未定义字段。
- 验收：service 命令层必须覆盖成功、未知 scene/element、非 slide、非法坐标和不改动
  actions；FastAPI 网关验证权限/revision/幂等照旧；Web 验证本地预览、保存后重读、409
  不重放；真实浏览器拖动一个元素，保存、刷新后坐标仍在，320px 无横向溢出。

#### 切片 C：文本就地编辑

- 参考：`components/element/ProsemirrorEditor.tsx`(553)、`element/TextElement/index.tsx`(228)、
  `lib/prosemirror/**`。
- 这是"能改内容"的关键一步；没有它，编辑器仍然只能看。

#### 切片 D：缩放 / 旋转 / 对齐线 / 标尺

- 参考：`hooks/useScaleElement.ts`、`useRotateElement.ts`、`AlignmentLine.tsx`、`Ruler.tsx`、
  `Operate/ResizeHandler.tsx`、`RotateHandler.tsx`。

#### 切片 E：元素增删改

- 参考：`Editor/Canvas/hooks/useInsertFromCreateSelection.ts`、`ShapeCreateCanvas.tsx`、
  `Operate/**` 的各类型菜单、`Fixins`/`ActionsBar` 里的元素操作。

---

## 优先级 2 · 课堂行为补齐

参考 `components/edit/PlaybackChromeRoot.tsx`（1991 行）的状态机。
移植层现在只有外壳与圆桌，**行为层基本没搬**。

| 要做 | 参考位置 |
| --- | --- |
| 键盘快捷键：方向键翻页、Esc 退出全屏、Space 播放/暂停 | `PlaybackChromeRoot.tsx` 里的 `onKeyDown` 处理器 |
| 演示 / 全屏模式（`isPresenting`）：隐藏头栏与工具栏、`cursor-none` | 同上；移植层 `MaicClassroomShell` 已接受 `isPresenting` / `onTogglePresentation` 参数，只是没人传 |
| 自动播放：按场景的 action 时间线逐条执行（speech / spotlight / laser / 白板 / widget_*） | `PlaybackChromeRoot.tsx` + `lib/playback/**`(8 文件) |
| 场景切换守卫（课程完成时、生成中的场景） | `PlaybackChromeRoot.tsx` 的 `gatedSceneSwitch` |

**注意**：`action` 的数据已经在播放计划里（`scene.steps`、`scene.dropped_actions`），
`webreact/src/features/openmaic/playerModel.js` 的 `actionTimeline()` 已经会解析。

---

## 优先级 3 · 更多幻灯片版式

现在 `openmaic-service/src/dsl/slide-canvas.ts` 只有 3 种版式：
`title`（纯标题）/ `bullets`（要点）/ `sections`（2~3 张卡片）。

补：封面页、目录页（编号列表）、双栏对比、要点+结论条。
**约束**：合成必须是纯函数且确定性，几何不变量（元素在画布内、页眉不与正文重叠、
id 唯一、文本自带内联字号）必须继续被 `tests/slide-canvas.test.mjs` 钉住。

---

## 优先级 4 · 图表 / 公式 / 代码高亮

安装 `echarts` / `katex` / `shiki`，并接上已预留的插口：

- `setLatexRenderer((latex, opts) => html)` —— 来自 `slide/index.js` 与 `scene/index.js`
- `setCodeHighlighterFactory(...)` —— 来自 `slide/index.js`
- 图表：`slide/components/element/ChartElement/Chart.jsx` 现在是手写 SVG 降级实现，
  换成 echarts 要保留同样的外层盒子与 `getChartOption` 的输出。

**先确认生成侧会产出这些元素再动手**：`slide-canvas.ts` 目前只合成 text 与 shape，
所以装了也用不上——除非你先让生成器产出 latex / code / chart 元素。

---

## 优先级 5 · 工具栏缺失控件

参考 `components/canvas/canvas-toolbar.tsx` 还有：白板、元素拾取、演示、
静音/音量、停止讨论。各自依赖不同的 store / 运行时。

**逐个评估**：有对应运行时（例如"演示"只需要 `isPresenting` 状态）就做；
没有的（白板、元素拾取）**不要放死按钮**。

---

## 优先级 6 · 模板后台与运行时闭环（待移植，不算完成）

用户已确认历史并发会话结束；tts/jobs/provider 的旧文件占用限制不再作为阻塞理由。
开工时已有未提交文件仍按 `git status` 保留，不纳入本任务提交。

以下按模板源码核对，实施时每项继续拆成独立可验收切片：

| 能力 | 模板依据 | 必须补齐的闭环 |
| --- | --- | --- |
| PBL 工作区 | `app/api/pbl/v2/{instructor,evaluate,open-task,simulator,task/update}/route.ts`、`lib/pbl/v2/**`、`components/scene-renderers/pbl/**` | 导师 SSE、任务打开/更新、提交与评估、模拟器、学习进度持久化和刷新恢复；所有请求绑定当前用户/课程/舞台 |
| 圆桌与课堂会话 | `app/api/chat/{route.ts,pi/route.ts}`、`components/roundtable/**`、`lib/audio/**` | 实时多角色流、逐角色音频、停止与恢复；复用已有场景讲解管线并保持内容归属 |
| 测验判分与学习记录 | `app/api/quiz-grade/route.ts`、模板场景与持久化模块 | 客观题与简答题、反馈、课程完成状态、持久化与隔离 |
| 工作台 Agent 会话 | `app/api/agent/sessions/**`、`app/api/agent/{runtime,owner-events}/route.ts`、`components/workbench/**` | 持久会话、消息、事件流、任务取消、恢复、编辑命令与舞台版本一致性 |
| 模型、媒体与资料能力 | `app/api/server-providers/route.ts`、`app/api/verify-*/route.ts`、`app/api/generate/**`、`app/api/transcription/route.ts`、`app/api/{parse-pdf,extract-document,web-search}/route.ts` | 配置检测、真实生成/解析、语音输入、可用性与失败恢复；凭据只驻服务端 |
| 资源、发布与导出 | `app/api/stages/**`、`app/api/persistence/**`、`app/api/classroom-media/**`、`app/api/export-video/**` | 版本/资源/发布语义、归档恢复、媒体下载与完整导出；保留现有鉴权、净化和限额 |

最终须逐条清点模板 API、界面和运行时，并在能力矩阵标明等价实现、适配差异、
测试及真实浏览器证据。环境缺凭据可记为“未验证”，不可写成“功能已完成”。

### 当前尚未接通的入口

| 功能 | 当前缺口 |
| --- | --- |
| PBL 的 workspace 阶段（点「开始项目」之后） | 待补 `chat` / `submission` / `use-instructor-stream`（SSE）/ 持久化与设置适配；接通前保持显式占位 |
| 圆桌的实时流式、逐智能体 TTS、语音输入（ASR）、真人头像图 | 待补对应后台与资源适配；接通前不渲染死按钮，头像暂用名字首字 |

这两块在交付说明里必须如实标为未完成，不能用“架构上做不了”代替后端移植。
