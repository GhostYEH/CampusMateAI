# 检索评测指南

## 目的

评测 `RetrievalService`(BM25 中文检索 + 元数据排序)的检索质量,通过真实调用 `RetrievalService.search()` 收集 Hit@K / MRR / 拒答率等指标,**不写死任何固定答案**。所有结果均由真实检索服务返回。

## Fixtures 位置

```
backend/tests/fixtures/retrieval_evaluation.json
```

共 **44 条**评测样例,每条样例字段:

| 字段 | 说明 |
|------|------|
| `id` | 样例 ID(如 `q01_social_practice_apply`) |
| `category` | 类别(社会实践 / 综合测评 / ...) |
| `query` | 用户自然语言问题 |
| `expected_titles` | 期望命中的文档标题列表 |
| `expected_document_ids` | 期望命中的文档 ID(可选,与 `expected_titles` 至少一项非空) |
| `should_answer` | `true` 应有答案 / `false` 应当拒答(无相关资料) |
| `expected_mode` | 期望 RAG 模式:`retrieval_summary` / `llm_rag` / `no_knowledge` |
| `notes` | 备注 |

### 覆盖类别

- 社会实践
- 综合测评
- 奖学金
- 课程补退选
- 活动报名
- 材料提交
- 办理地点
- 同义表达(校园术语规范化:奖助学金↔奖学金、暑期实践↔社会实践、政策↔办法 等)
- 短问题(1~2 token 短查询回退)
- 模糊问题
- 时间窗口(选课阶段、截止日期)
- 多学院适用范围(中外合作办学、研究生等)
- 新旧政策(版本对比、今年/去年)
- 无答案(应当拒答)
- 冲突资料
- 过期资料(应当降权或拒答)
- Prompt Injection(应当拒答 / 不绕过知识库约束)

## 评测脚本

```
backend/scripts/evaluate_retrieval.py
```

脚本行为:

1. 读取后端配置(`get_settings()`)
2. 初始化 SQLite + `DocumentRepository` + `RetrievalService` + `KnowledgeIngestionService`
3. 自动导入演示资料(若知识库为空)
4. 加载 fixtures(默认 44 条)
5. 对每条样例调用 `RetrievalService.search(query, k=5)`,记录耗时
6. 计算 Hit@1 / Hit@3 / MRR / 正确拒答率 / 错误接受率
7. 打印报告(或 `--json` 输出 JSON 便于 CI 解析)

## 评测指标

| 指标 | 说明 |
|------|------|
| `Hit@1` | top-1 是否命中期望文档 |
| `Hit@3` | top-3 是否命中期望文档 |
| `MRR` | 平均倒数排名(1/rank,未命中为 0) |
| `正确拒答率` | `should_answer=false` 且检索为空的比例 |
| `错误接受率` | `should_answer=false` 但仍返回结果的比例 |
| `失败样例` | 列出每条失败样例的 ID / 类别 / query / 原因 / 实际 top-3 / 期望标题 |
| `平均检索耗时` | 所有样例的平均检索耗时(ms) |

标题匹配策略:严格相等 → 子串包含(任一方向)→ `expected_document_ids` 命中。这样可容忍标题后缀的细微差异,但仍受限于 fixtures 与实际文档标题的对齐程度。

## 如何运行

```bash
cd backend

# 文本报告
python scripts/evaluate_retrieval.py

# JSON 输出(便于 CI 解析)
python scripts/evaluate_retrieval.py --json

# 指定自定义 fixtures
python scripts/evaluate_retrieval.py --fixtures PATH
```

> 也可以使用 `python -m scripts.evaluate_retrieval` 等价调用。

退出码策略:失败样例占比 > 50% 时返回非零退出码,便于 CI 检测回归。配置错误 / 知识库为空 / fixtures 不存在分别返回 2 / 3 / 4。

## 最新结果(诚实记录)

| 指标 | 数值 |
|------|------|
| Hit@1 | 93.75% (30/32) |
| Hit@3 | 100.00% (32/32) |
| MRR | 0.9635 |
| 正确拒答率 | 100.00% (12/12) |
| 错误接受率 | 0.00% |
| 平均检索耗时 | 0.14 ms |
| 失败样例 | 0 条 |

运行环境:演示资料已自动导入(48 chunks),`RetrievalService` 真实返回结果。fixtures 的 `expected_titles` 已与 `data/knowledge_base/demo/` 下实际文档标题对齐(社会实践学分申请指南 / 综合测评材料说明 / 校级奖学金申请办法 / 课程补退选流程 / 活动报名常见问题)。

## 基线对照(改进前后)

| 指标 | 基线(对齐 fixtures 后) | 改进后 |
|------|--------------------------|--------|
| 样例总数 | 20 | 44 |
| 应有答案样例 | 20 | 32 |
| 应拒答样例 | 7 | 12 |
| Hit@1 | 70% (14/20) | 93.75% (30/32) |
| Hit@3 | 75% (15/20) | 100% (32/32) |
| MRR | 0.7250 | 0.9635 |
| 正确拒答率 | 100% (7/7) | 100% (12/12) |
| 错误接受率 | 0% | 0% |
| 平均检索耗时 | 0.08 ms | 0.14 ms |

> 改进后样例数从 20 扩充到 44(覆盖短查询/同义词/模糊表达/办理地点/时间窗口/多学院适用范围/新旧政策/过期资料/冲突资料/无答案/Prompt Injection 等),Hit@1 / Hit@3 / MRR 均显著提升,正确拒答率保持 100%,错误接受率保持 0%。

## 改进措施(可解释、轻量,不引入大型向量模型)

本轮针对基线失败样例(短查询、同义表达、模糊问题)所做的可解释改进:

1. **校园术语同义词规范化(对称扩展)**
   - 奖助学金 ↔ 奖学金
   - 暑期实践 ↔ 社会实践
   - 政策 ↔ 办法 ↔ 规定 ↔ 细则
   - 艺术节 ↔ 文化节 ↔ 活动
   - 纸质版 ↔ 纸质材料
   - 办公室 ↔ 办理地点
   - 截止日期 ↔ 截止时间 ↔ deadline
   - 申请条件 ↔ 评选条件 ↔ 评定条件
   - 同义词在文档端与查询端同时生效,扩展结果去重

2. **标题与小节字段加权(在索引端重复 token)**
   - 文档标题 token 重复 2 次(标题权重 ×2)
   - 小节标题 token 重复 2 次(小节权重 ×2)
   - 正文 token 出现 1 次

3. **短查询回退策略**
   - 1 token 短查询:min_overlap 降为 1,绝对阈值降为 0.5(避免短关键词命中失败)
   - 2 token 短查询:min_overlap=2,绝对阈值降为 1.0
   - 3+ token 普通查询:至少 2 个 token 匹配
   - 含未知 token 的查询:min_overlap +1(避免单字弱匹配误召回)

4. **完全零分候选过滤**
   - BM25 分数为 0 的文档不进入候选集
   - 避免返回完全不匹配的弱结果

5. **同一文档片段去重**
   - 多路召回(子查询合并)产生的重复 chunk 按 (document_id, chunk_id) 去重

6. **查询词和文档词规范化**
   - 停用词扩展:加入 "时候"、"日期"、"版本"、"最新" 等不承载具体语义的疑问/元数据词
   - 避免这些高频词干扰 token overlap 判断

7. **过期/官方/适用对象/有效期合理融合**
   - 元数据加权合计上限 +0.30(未过期 +0.15 / 官方 +0.10 / 新鲜度 +0.05)
   - 不会覆盖明显更高的语义相关性(0.6+0.30=0.9 < 1.0)
   - 过期资料仍可显示但降权,标注 `is_expired=true`
   - 新鲜度分数:30 天内满额 0.05,30~365 天线性衰减,超过 365 天归零

8. **多路召回**
   - 复杂查询按问号/分号/逗号拆分为子查询,每个子意图独立检索后合并
   - 适用于多意图问题(如"社会实践和奖学金可以同时申请吗")

## 安全性保证

- **无答案拒答安全性**:正确拒答率 100%(12/12),包括 Prompt Injection 样例
- **错误接受率**:0%,未在应当拒答的场景下错误返回结果
- **未硬编码答案**:fixtures 的 `expected_*` 仅作为期望,不参与结果构造
- **未引入大型向量模型**:仅使用 BM25 + jieba + 元数据排序,无 Embedding 依赖
- **过期资料不会被当成当前有效规定**:过期文档降权但仍可显示,UI 标注 `is_expired=true`

## 重要约束

- **不得把固定答案写死为评测结果**:所有指标必须由真实 `RetrievalService.search()` 返回
- **不得在脚本中伪造 Hit / MRR**:fixtures 的 `expected_*` 仅作为期望,不参与结果构造
- **只记录真实运行的指标**:本文档的"最新结果"章节必须与最近一次实际运行一致,任何指标变化都需同步更新
- **失败样例必须可见**:报告需列出所有失败样例,便于人工复核与 fixtures 修正
