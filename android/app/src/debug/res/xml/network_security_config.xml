<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may point at an emulator bridge or a LAN development server.
         Release continues to use the HTTPS-only main configuration. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
