# Web 专注二级体验 QA

- 范围：仅 `web/` 专注页；未修改 Android、Harmony、微信端。
- 六类入口：沉浸专注、AI 路线、指标洞察、记录复盘、趋势分析、任务编辑均已接通。
- 状态：开始/暂停/结束、AI 拆解、任务保存/完成复用现有 API；接口失败保留当前界面并显示错误。
- 输入：遮罩、关闭按钮、Escape；主要入口支持键盘 Enter 和可见焦点。
- 动效：卡片扩展、轨道呼吸、错峰时间线、柱形生长、印章落位、磁吸按钮、完成粒子。
- 降级：`prefers-reduced-motion` 禁用动画和磁吸位移。
- 响应式：720px 以下切换为单列，动态层使用 `100dvh` 限制并可滚动。
- 验证：专注页测试通过，Vite 生产构建通过，`git diff --check` 通过。
