# Bind to loopback or an internal network interface only.
OPENMAIC_HOST=127.0.0.1
OPENMAIC_PORT=4010
OPENMAIC_DATABASE_URL=
# Inject through a secret manager in deployments; never commit a value.
OPENMAIC_INTERNAL_SECRET=
