# CampusMateAI CNN 面部表情识别工程依赖
# Python >= 3.10
#
# 安装：
#   pip install -r requirements.txt
#
# 若需 TFLite 导出，额外安装（可选，环境支持时才用）：
#   pip install onnx2tf tensorflow ai-edge-litert sng4onnx onnxgraphsurgeon
#
# 若需 GPU 训练，请按本机 CUDA 版本安装对应 torch/torchvision：
#   https://pytorch.org/get-started/locally/

# 核心训练与模型
torch>=2.1
torchvision>=0.16

# 数据处理
numpy>=1.24
pandas>=2.0
Pillow>=10.0

# 配置
PyYAML>=6.0

# ONNX 导出与推理
onnx>=1.14
onnxruntime>=1.16

# 评估与可视化
matplotlib>=3.7

# 测试
pytest>=8.0

# === 可选：TFLite 导出链路（环境支持时启用）===
# onnx2tf>=1.20
# tensorflow>=2.15
# ai-edge-litert>=1.0
